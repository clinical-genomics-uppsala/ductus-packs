---
name: run_analysis_miarka_action
description: >
  run a pipeline 
enabled: true
runner_type: orquesta
entry_point: workflows/run_analysis_miarka.yaml
pack: ductus
parameters:
  context:
    default: {}
    immutable: true
    type: object
  workflow:
    default: ductus.run_analysis_miarka
    immutable: true
    type: string
  process_host_proj_path:
    required: true
    type: string
    default: "{{ config_context.process_host_proj_path }}"
  process_host_url:
    required: true
    type: string
    default: "{{ config_context.process_host_url }}"
  job_status_url:
    required: true
    type: string
    default: "{{ config_context.job_status_url }}"
  process_host_gateway_port:
    required: true
    type: string
    default: "{{ config_context.process_host_gateway_port }}"
  gateway_api_key:
    required: true
    type: string
    default: "{{ config_context.miarka_api_key }}"
  analysis_name:
    required: true
    type: string
  workpackage:
    required: true
    type: string
  analysis:
    required: true 
    type: string
  process_settings:
    required: true 
    type: object
    default: "{{ config_context.process_settings_miarka }}"
  processing_api_access_key:
    required: true
    type: string
    default: "{{ config_context.processing_api_access_key }}"
    secret: true
  processing_api_service_url:
    required: true
    type: string
    default: "{{ config_context.processing_api_service_url }}"
  processing_api_analysis_update_status_url:
    required: true
    type: string
    default: "{{ config_context.processing_api_analysis_update_status_url }}"
  mail_bioinfo:
    required: true
    type: string
    default: "{{ config_context.notification_mail_bioinfo }}"
  mail_settings:
    required: true
    type: object
    default: "{{ config_context.notification_mail }}"
