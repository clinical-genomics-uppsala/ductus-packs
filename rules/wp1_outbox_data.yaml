---
name: "auctornotitia.wp1_outbox_data"
description: "..."
enabled: true

trigger:
    type: "arteria.runfolder_ready"

criteria:
    trigger.runfolder:
      type: "regex"
      pattern: "^.+/OUTBOX/.+$"

action:
    ref: "auctornotitia.retrieve_wp1_result"
    parameters:
        host: "{{trigger.host}}"
        runfolder: "{{trigger.runfolder}}"