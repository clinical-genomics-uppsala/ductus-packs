---
name: "wp1_outbox_data"
pack: "auctornotitia"
description: "..."
enabled: false

trigger:
    type: "arteria.runfolder_ready"

criteria:
    trigger.runfolder:
      type: "contains"
      pattern: "wp1/ngs/klinik/OUTBOX"

action:
    ref: "auctornotitia.retrieve_wp1_result"
    parameters:
        host: "{{trigger.host}}"
        runfolder: "{{trigger.runfolder}}"